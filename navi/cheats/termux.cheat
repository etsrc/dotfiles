% termux

# pkg install navi tmux httpie go libqrencode rust -y

# ln -s ./storage/shared/Sync ~/Sync
# mv my-favorite-font.ttf ~/.termux/font.ttf

# sshd
# pkill sshd
# ~/.ssh/authorized_keys'
# ssh -p 8022 192.168.1.167

# termux:X11 

# Add internal storage to termux
termux-storage-get

# Change repo
termux-change-repo

# Termux Version
echo $TERMUX_VERSION

# pkg search
pkg search <query>

# pkg install
pkg install <package>

# pkg upgrade
pkg upgrade

# pkg update
pkg update

# Connect to Android
ssh -p 8022 u0_a437@192.168.1.165

# Copy Sync Directory
scp -P 8022 -r u0_a437@192.168.1.165:storage/shared/Sync ~/
